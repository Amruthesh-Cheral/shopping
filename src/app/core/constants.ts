export enum ApiEndPoints {
    // auth
    signIn = 'auth/login',
    signUp = 'auth/signUp',
    profileGet = 'profile/get',

    // products add
    products = 'products',
    wCartUserid = 'wCart?userId='
    
}